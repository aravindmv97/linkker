import React, { FC } from "react";
import styled from "styled-components";

const LinkSearchWrapper = styled.div``;

export default function LinkSearch() {
  return <LinkSearchWrapper>
  </LinkSearchWrapper>;
}
